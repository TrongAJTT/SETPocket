name: my_multi_tools
description: A multi-platform application with various tools
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  path_provider: 2.1.2
  intl: 0.20.2
  shared_preferences: 2.2.2
  crypto: ^3.0.3
  flutter_localizations:
    sdk: flutter  
  flutter_gen: 5.10.0
  logger: 2.5.0
  quick_actions: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true