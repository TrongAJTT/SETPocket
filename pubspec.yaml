name: my_multi_tools
description: Multi Tools - A multi-platform application with various utilities
publish_to: 'none'
version: 1.0.2+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  path_provider: 2.1.2
  intl: 0.20.2 
  shared_preferences: 2.2.2
  crypto: ^3.0.3
  http: ^1.1.0
  live_currency_rate: ^1.0.5
  fl_chart: ^0.65.0
  math_expressions: ^2.6.0
  decimal: ^3.2.1
  flutter_localizations:
    sdk: flutter  
  flutter_gen: 5.10.0  
  logger: 2.5.0
  logging: ^1.2.0
  quick_actions: ^1.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
  generate: true